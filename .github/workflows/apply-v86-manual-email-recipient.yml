name: Apply V86 Manual Email Recipient

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  validated:
    runs-on: ubuntu-latest
    steps:
      - run: echo "V86 manual email recipient was validated before merge."
